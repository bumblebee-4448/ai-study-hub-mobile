/**
 * Profile Tab — delegates entirely to the Profile feature's screen.
 * Business logic lives in features/profile/, not here.
 */
import { ProfileScreen } from "@/features/profile";

export default ProfileScreen;
